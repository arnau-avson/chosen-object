services:
  web:
    build: ./web
    ports:
      - "3001:3001"
    restart: unless-stopped

  backend:
    build: ./backend
    ports:
      - "8002:8002"
    env_file:
      - ./backend/.env
    environment:
      - APP_ENV=production
      - APP_PORT=8002
    restart: unless-stopped
